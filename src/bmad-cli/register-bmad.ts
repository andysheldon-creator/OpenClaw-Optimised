export function registerBMadCommand(program: any): void {
  // noop stub for command registry to load during development builds.
  // Full implementation can live in a separate package or be added here.
}
